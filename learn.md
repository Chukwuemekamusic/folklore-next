# Things to learn and document

- [] novel
- [] uploadthing


## Date Format
- [ ] how to format the date in the table view
```javascript
{new Intl.DateTimeFormat("en-US", { dateStyle: "medium" }).format(story.createdAt)}
```